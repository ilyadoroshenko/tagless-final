# ReqRes.in integration app

To start the service:
* Create `application.conf` from `application.conf.example`
* Create table `users` in your Postgres using `db.sql` from `resources` folder
* `sbt ~reStart`
